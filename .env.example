# Optional local overrides for Docker Compose and the Node server.
# Copy to `.env` if you want to use email notifications or custom local settings.

PORT=3333
NODE_ENV=production
MONGO_URL=mongodb://mongodb:27017/hal_tejas_db
GMAIL_USER=
GMAIL_PASS=
